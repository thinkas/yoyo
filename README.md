项目简介
是一个用于自动检测正则表达式语义错误的测试生成工具。不同于传统方法仅依赖随机生成或弱覆盖标准。本项目首次将：
最小化 DFA（deterministic finite automaton）建模
Prime Path Coverage（主路径覆盖）强覆盖标准
最小流网络优化
路径相似聚类（结构相似度）
Pairwise Testing（成对覆盖）字符交互约简系统结合。
该工具可显著提升对语义错误的检测能力
错误处理重复、边界情况
错误的字符类组合
结构性漏匹配或过匹配
错误的 Kleene 操作含义
边界路径失覆盖导致的匹配行为偏差等。
